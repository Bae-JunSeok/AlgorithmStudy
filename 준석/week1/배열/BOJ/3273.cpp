#include <bits/stdc++.h>

using namespace std;

int main(){
    int a, c;
    cin >> a;
    int count = 0;
    int arr[a];
    int arr_2[a];
    for(int i = 0; i < a; i++)
        cin >> arr[i];
    cin >> c;
    for(int i = 0; i < a; i++){
    }
    cout << count;
    return 0;
}